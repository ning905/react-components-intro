import { Buttons } from "./Buttons";
import { SearchBar } from "./SearchBar";
import { Logo } from "./Logo";

export const Main = () => {
  return (
    <main>
      <Logo />
      <SearchBar />
      <Buttons />
    </main>
  );
};
